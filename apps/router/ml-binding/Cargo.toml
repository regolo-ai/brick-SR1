[package]
name = "ml_spatial_router"
version = "0.1.0"
edition = "2021"
description = "Traditional ML algorithms binding for spatial-router using Linfa"
license = "Apache-2.0"

[lib]
name = "ml_spatial_router"
crate-type = ["cdylib", "rlib"]

[dependencies]
# Linfa ML framework - inference only (training done in Python)
# Training: src/training/ml_model_selection/
linfa-nn = "0.8"           # Ball Tree for KNN/KMeans inference

# Linear algebra (must match Linfa's ndarray version)
ndarray = "0.16"

# Serialization
serde = { version = "1.0", features = ["derive"] }
serde_json = "1.0"

# Pin noisy_float to version compatible with Rust 1.75
noisy_float = "=0.2.1"

# FFI
libc = "0.2"

[profile.release]
lto = true
codegen-units = 1
opt-level = 3
