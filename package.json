{
  "name": "brick-monorepo",
  "private": true,
  "version": "2.1.0",
  "description": "Brick: multimodal LLM routing gateway. Monorepo containing router (Go+Rust), CLI (TypeScript), evals, training, paper.",
  "homepage": "https://github.com/regolo-ai/brick-SR1",
  "repository": {
    "type": "git",
    "url": "git+https://github.com/regolo-ai/brick-SR1.git"
  },
  "license": "Apache-2.0",
  "workspaces": [
    "apps/cli"
  ],
  "scripts": {
    "build:cli": "npm run --workspace=@regolo-ai/brick build",
    "test:cli": "npm run --workspace=@regolo-ai/brick test",
    "lint:cli": "npm run --workspace=@regolo-ai/brick lint",
    "version:cli": "npm view --workspace=@regolo-ai/brick"
  },
  "engines": {
    "node": ">=18.0.0"
  }
}
